[dependencies]

[package]
authors.workspace = true
categories = ["development-tools", "parser-implementations"]
description = "Generated node type enums and metadata from tree-sitter-rust"
documentation = "https://docs.rs/tss-rust/"
edition.workspace = true
homepage.workspace = true
keywords = ["ast", "codegen", "parser", "rust", "tree-sitter"]
license.workspace = true
name = "tss-rust"
readme = "README.md"
repository.workspace = true
version = "0.1.2"

[lib]
name = "tree_sitter_symbols_rust"
path = "src/lib.rs"

[features]
# Convenience features
default = ["meta-full"]
# full = ["*"] can we do this with the build script?
meta-full = [
  "meta-children",
  "meta-extra",
  "meta-fields",
  "meta-named",
  "meta-root",
  "meta-subtypes",
]

# Metadata features
meta-children = []
meta-extra = []
meta-fields = []
meta-named = []
meta-root = []
meta-subtypes = []

# Node type features (generated by build.rs)
# See generated documentation for full list
# <!-- generated-features-start -->
# <!-- generated-features-end -->

[package.metadata.cargo-machete]
ignored = ["ropey", "serde", "serde_json", "textum", "tree-sitter-rust"]

[build-dependencies]
ropey.workspace = true
serde.workspace = true
serde_json.workspace = true
textum.workspace = true
tree-sitter-rust.workspace = true

[lints.clippy]
all = "deny"
cargo = "deny"
pedantic = "deny"

[lints.rust]
unsafe_code = "forbid"
# missing_docs = "deny"
